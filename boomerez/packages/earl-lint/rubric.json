{
  "criterion": "CC-3",
  "description": "Every shipped HTML artifact passes this rubric with zero errors.",
  "rules": [
    { "id": "R1", "check": "doctype", "description": "Document declares <!doctype html>." },
    { "id": "R2", "check": "html-lang", "description": "<html> element has a non-empty lang attribute." },
    { "id": "R3", "check": "title-nonempty", "description": "Document has a non-empty <title>." },
    { "id": "R4", "check": "viewport-meta", "description": "Document has a viewport meta tag." },
    { "id": "R5", "check": "img-alt", "description": "Every <img> has a non-empty alt attribute." },
    { "id": "R6", "check": "tokens-linked", "description": "Document references the shared design tokens (tokens.css or --bz- custom properties)." }
  ]
}
