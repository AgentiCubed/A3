# @boomerez/tokens

Shared CSS design tokens for all Boomerez apps. `tokens.css` defines every
custom property under the `--bz-` prefix; apps reference tokens instead of
raw values (design standard criterion CC-2).

Starter palette and scales are provisional — tune them in one place here and
every app picks the change up on the next build.
